branches:
  except:
    - main
language: ruby
cache: bundler
sudo: false
rvm:
- 2.6.5
- 2.7.5
- 3.0.3
addons:
  apt:
    packages:
      - libarchive-dev
script:
- bundle exec rubocop
- bundle exec foodcritic -f any .
- bundle exec rspec
